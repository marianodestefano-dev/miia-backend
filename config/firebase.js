"use strict";
// Stub para tests — Firebase se inicializa en server.js via admin.initializeApp()
// Los modulos usan __setFirestoreForTests(db) en tests, nunca este fallback
module.exports = { db: null };
