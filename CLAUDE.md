# MIIA Backend — Instrucciones para Claude Code

## INSTRUCCIÓN PRINCIPAL
El archivo MAESTRO de instrucciones está en `../.claude/CLAUDE.md` (raíz del proyecto).
LEE ESE ARCHIVO SIEMPRE. Este archivo solo existe como puntero.

## STANDARD DE CÓDIGO
Google + Amazon + APPLE + NASA (fail loudly, exhaustive logging, design style, zero silent failures).
Siempre leyendo completamente CLAUDE.md y RESUMEN_EJECUTIVO_MIIA.md.
SIEMPRE EN ESPAÑOL.

## ZONA CRÍTICA DE WHATSAPP
Antes de tocar `server.js` o `tenant_manager.js`:
1. Explicar a Mariano qué se cambia y por qué
2. Esperar confirmación explícita
3. Prefijo en descripción Bash: `🚨🔴⚠️ ALERTA: ZONA CRÍTICA —`
